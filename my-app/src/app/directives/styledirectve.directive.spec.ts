import { StyledirectveDirective } from './styledirectve.directive';

describe('StyledirectveDirective', () => {
  it('should create an instance', () => {
    const directive = new StyledirectveDirective();
    expect(directive).toBeTruthy();
  });
});
